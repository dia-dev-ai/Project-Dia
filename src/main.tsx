import React from 'react'
import ReactDOM from 'react-dom/client'
import ParticleSphere from '../D.I.A visualizer'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ParticleSphere />
  </React.StrictMode>
)